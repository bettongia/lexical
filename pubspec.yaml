name: betto_lexical
description: >
  Lexical text utilities (tokenizer, stemmer, stopwords) for Dart and Flutter.
version: 0.1.0-dev.1
homepage: https://bettongia.github.io/lexical/
issue_tracker: https://github.com/bettongia/lexical/issues
repository: https://github.com/bettongia/lexical

topics:
  - indexing
  - search

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.12.0

dependencies:
  betto_icu: ^0.1.0-dev.1
  intl: ^0.20.2
  snowball_stemmer: ^0.1.0

dev_dependencies:
  betto_builder_tools: ^0.1.0-dev.1
  code_builder: ^4.11.1
  lints: ^6.0.0
  test: ^1.25.6
